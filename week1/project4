#include<stdio.h>

int main(void) {
	float fahrenheit, celsius;

	printf("화씨값을 입력하세요 : "); //화씨값을 입력받는다
	scanf("%f", &fahrenheit);

	celsius = (5.0 / 9.0) * (fahrenheit - 32); //섭씨값 계산식
	printf("변환된 섭씨값은 %f입니다.", celsius); //float형으로 출력
	
	return 0;
}
